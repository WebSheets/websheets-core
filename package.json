{
  "name": "websheets-core",
  "version": "0.1.3",
  "description": "Back-end components for websheets",
  "main": "index.js",
  "scripts": {
    "build": "./node_modules/.bin/babel src --out-dir build --source-maps --no-babelrc --presets es2015",
    "prepublish": "rm -rf build/* && npm run build",
    "test": "./node_modules/.bin/mocha --recursive"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-polyfill": "^6.7.2",
    "babel-preset-es2015": "^6.6.0",
    "babel-register": "^6.7.2",
    "mocha": "^2.4.5"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/WebSheets/websheets-core.git"
  },
  "keywords": [
    "spreadsheets",
    "websheets"
  ],
  "author": "Matt Basta <me@mattbasta.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/WebSheets/websheets-core/issues"
  },
  "homepage": "https://github.com/WebSheets/websheets-core#readme",
  "dependencies": {
    "websheets-engine": "^0.1.0"
  }
}
